import type { Locale } from '../config'

// Lead Machine / Ads namespace (lm_ads). Keys prefixed `lm.`.
type Dict = Record<string, string>

export const lm_ads: Record<Locale, Dict> = {
  en: {},
  ar: {},
  ru: {},
}
