import type { Locale } from '../config'

// Lead Machine / Ads namespace (lm_core). Keys prefixed `lm.`.
type Dict = Record<string, string>

export const lm_core: Record<Locale, Dict> = {
  en: {},
  ar: {},
  ru: {},
}
