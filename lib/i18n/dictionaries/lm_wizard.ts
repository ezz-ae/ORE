import type { Locale } from '../config'

// Lead Machine / Ads namespace (lm_wizard). Keys prefixed `lm.`.
type Dict = Record<string, string>

export const lm_wizard: Record<Locale, Dict> = {
  en: {},
  ar: {},
  ru: {},
}
